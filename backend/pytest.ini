[pytest]
DJANGO_SETTINGS_MODULE = pconfig.settings
python_files = test_*.py
addopts = -ra --strict-markers
filterwarnings =
    # WhiteNoise warns when collectstatic has not been run, which tests do not need.
    ignore:No directory at:UserWarning
